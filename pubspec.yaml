name: pibd_31_gutorov_i_a_pmd
description: "Flutter labs project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_svg: ^2.2.3

  dio: ^5.9.0
  json_annotation: ^4.9.0
  pretty_dio_logger: ^1.4.0

  equatable: ^2.0.7
  flutter_bloc: ^9.1.1
  copy_with_extension: ^10.0.1
  copy_with_extension_gen: ^10.0.1

  intl: ^0.20.2
  flutter_localization: ^0.3.3

  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  flutter_launcher_icons: ^0.14.4

  build_runner: ^2.10.4
  json_serializable: ^6.11.2

flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/book-store-logo.png"
  min_sdk_android: 21

flutter:
  generate: true

  uses-material-design: true

  assets:
    - assets/svg/